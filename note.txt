basic route53 construct to return a TXT record containing the nearest AWS region

will deploy 2 instances one with the default regions, one will all the opt in regions

expose as lib to allow users to consume this themselves and set the regions themselves 
-> have blue green consume this lib
